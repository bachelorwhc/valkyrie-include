#ifndef _VALKYRIE_THREAD_MANAGER_H 
#define _VALKYRIE_THREAD_MANAGER_H 
#include "common.h"

namespace Valkyrie {
	class ThreadManager {
	public:
		ThreadManager();
		virtual ~ThreadManager();

	private:

	};
}

#endif